from setuptools import setup, find_packages

setup(
    name='syscfg',
    version='0.1',
    description='TAF System Configuration',
    author='',
    author_email='',
    url='',
    install_requires=[
    ],
    packages=find_packages(),
    zip_safe=False,
)
